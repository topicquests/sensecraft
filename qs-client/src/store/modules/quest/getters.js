//import state from "state";
/*
export default function() {
  getters: {
    isAuthenticated: state => state.isAuthenticated,
    canEdit: state => state.canEdit,
    isAdmin: state => state.canEdit,
    getNode: state => state.canEdit,

  };
  isAuthenticated () {
    return true; //state => state.isAuthenticated,
  };
  canEdit() {
    return true; //state => state.canEdit,
  };
  isAdmin() {
    return truel //state => state.isAdmin,
  };
  getNode(nodeId) {
    var node = {};
    node.nodeId = 'foo';
    node.label = 'Why is the sky blue?';
    node.type = 'Question';
    node.questions = [];
    node.answers = [];
    node.pros = [];
    node.cons = [];
    node.refs = []; // this is new

;  }
  
  //treeView: state => state.treeView,
  //isQuestView: state => state.isQuestView,

}*/
